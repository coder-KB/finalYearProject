import React from "react";
import "./Loading.css";

const Loading = () => {
    return (
        <div className="Loading">
            <h2>Generating Time Table ...</h2>
            <div className="Kinetic"></div>
        </div>
    );
};

export default Loading;
